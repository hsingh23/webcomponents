{
  "name": "bento-grid-generator",
  "version": "1.1.0",
  "description": "Generates a Bento grid from HTML files, extracting documentation from comments.",
  "main": "generate-index.js",
  "scripts": {
    "start": "node generate-index.js && echo 'index.html generated. Open it in your browser.'",
    "generate": "node generate-index.js"
  },
  "dependencies": {
    "fs-extra": "^11.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
