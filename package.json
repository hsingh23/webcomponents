{
  "name": "bento-grid-generator",
  "version": "1.2.0",
  "description": "Generates a demos.json file from HTML files in the demos directory.",
  "main": "generate-index.js",
  "scripts": {
    "start": "npm run generate && echo 'demos.json generated. Open index.html in your browser.'",
    "generate": "node generate-index.js"
  },
  "dependencies": {
    "fs-extra": "^11.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
